<!DOCTYPE html>
<html lang="en">
  <head>
    <%= display_meta_tags site: "Coco's Stained Glass",
                          separator: "—",
                          reverse: true %>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "public", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "portal", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="portal-body">
    <%= render "shared/navigation" %>

    <main id="main-content" class="portal-main">
      <div class="container">
        <%= render "shared/flash_messages" %>

        <header class="portal-header">
          <p class="portal-welcome">Welcome back, <%= current_user.first_name %></p>
          <h1 class="portal-title"><%= @page_title || "My Dashboard" %></h1>
        </header>

        <%= yield %>
      </div>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
